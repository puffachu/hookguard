# Contributing

1. Install Node.js 20+.
2. Run `npm install`.
3. Keep changes focused and covered.
4. Run `npm run lint && npm test && npm run test:coverage && npm run build`.
5. For oracle changes, include Foundry tests and gas snapshots when Foundry is installed.
